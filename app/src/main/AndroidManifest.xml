<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jamsecure">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".User_Select_Jam_Studio"></activity>
        <activity android:name=".User_Select_Location" />
        <activity android:name=".ChooseUserType" />
        <activity android:name=".RoomOwner" />
        <activity android:name=".NewJam" />
        <activity android:name=".HelpBot" />
        <activity android:name=".owner_register" />
        <activity android:name=".Register" />
        <activity android:name=".Bookjamroom" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>